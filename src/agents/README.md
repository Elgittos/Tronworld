# Agents

Agent-owned systems live here. This folder should decide what an AI avatar can sense, remember, consider, want, plan, and request.

It must not own world truth, UI, rendering, physics, or LLM provider configuration. Agents read world state and request validated actions through `src/actions`.

