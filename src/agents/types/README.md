# Types

Shared agent-only types live here.

Keep world truth types in `src/world`. Keep action request and validation types in `src/actions`. This folder is for agent snapshots, memories, affordances, scores, and plans.

