# LLM Integration Placeholder

AI and LM Studio integration intentionally come later.

The current app routes human commands through `src/actions/actions.ts`, so future AI agents can submit validated JSON actions through the same action system instead of mutating world state directly.
